/** Shared animation constants — mirrors CSS tokens in globals.css */

export const EASE: [number, number, number, number] = [0.22, 1, 0.36, 1];

export const DURATION = {
  quick: 0.2,
  base: 0.65,
  slow: 0.9,
} as const;
